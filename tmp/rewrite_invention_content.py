from __future__ import annotations

import os
import zipfile

from docx import Document
from docx.oxml import OxmlElement
from docx.text.paragraph import Paragraph


DOCX_PATH = r"D:\我的专利（1）\ALNS说明书 .docx"


def paragraph_text(paragraph: Paragraph) -> str:
    return paragraph.text.strip().replace("\u3000", " ")


def insert_after(paragraph: Paragraph, text: str) -> Paragraph:
    new_p = OxmlElement("w:p")
    paragraph._p.addnext(new_p)
    new_para = Paragraph(new_p, paragraph._parent)
    if paragraph.style is not None:
        new_para.style = paragraph.style
    if text:
        new_para.add_run(text)
    return new_para


def remove_paragraph(paragraph: Paragraph) -> None:
    element = paragraph._element
    parent = element.getparent()
    if parent is not None:
        parent.remove(element)


def find_exact(paragraphs: list[Paragraph], text: str) -> int:
    for index, paragraph in enumerate(paragraphs):
        if paragraph_text(paragraph) == text:
            return index
    raise ValueError(f"Paragraph not found: {text}")


def validate(path: str) -> dict[str, int | bool]:
    with zipfile.ZipFile(path) as zf:
        xml = zf.read("word/document.xml").decode("utf-8")
    return {
        "omath_count": xml.count("<m:oMath") + xml.count("<m:oMathPara"),
        "has_raw_latex_backslash": "\\" in xml,
        "paragraph_count": xml.count("<w:p"),
    }


def main() -> None:
    if not os.path.exists(DOCX_PATH):
        raise FileNotFoundError(DOCX_PATH)

    before = validate(DOCX_PATH)
    doc = Document(DOCX_PATH)
    paragraphs = doc.paragraphs

    invention_idx = find_exact(paragraphs, "发明内容")
    drawings_idx = find_exact(paragraphs, "附图说明")
    anchor = paragraphs[invention_idx]

    for paragraph in list(paragraphs[invention_idx + 1:drawings_idx]):
        remove_paragraph(paragraph)

    content = [
        "[0006] 本发明的目的在于克服现有空地协同配送方法中接力点固定、评分逻辑与执行过程不一致、无人机部署成本容易被低估、异常回退缺乏统一判断依据等不足，提供一种基于ALNS的无人机无人车协同绿色配送优化方法。该方法面向当前项目中的取送货任务、无人机、无人车和配送环境对象，统一生成直接配送候选方案和无人机无人车接力配送候选方案，并通过同一评价器完成候选筛选、路径插入、ALNS修复和执行阶段回退判断。",
        "[0007] 相较于仅按照静态中继点或单一距离指标进行任务分配的方法，本发明至少作出以下改进：第一，接力候选点由无人车当前位置、任务起点、任务终点、任务走廊采样点以及无人车到任务走廊的投影点共同生成，并与具体无人车绑定；第二，将无人机从当前位置飞往中继点的部署段纳入接力方案评价，避免默认无人机已经位于中继点；第三，将时间成本、无人机能耗、无人车能耗、碳排放估计、等待惩罚和回退风险纳入统一综合成本；第四，在ALNS求解中采用候选池、破坏算子、修复算子和自适应算子权重，提高联合搜索效率和解的稳定性；第五，在执行阶段复用同一评价器进行中继点重选或直接配送回退。",
        "[0008] 本发明的目的是通过以下技术方案来实现的：",
        "[0009] 一种基于ALNS的无人机无人车协同绿色配送优化方法，包括以下步骤：",
        "[0010] 步骤一：获取配送任务集合、无人机集合、无人车集合以及配送环境约束信息，建立空地协同配送基础数据模型。",
        "[0011] 步骤二：针对任务与无人机组合构建候选服务方式集合，所述候选服务方式包括无人机直接配送方式和无人机无人车接力配送方式。",
        "[0012] 步骤三：对候选服务方式进行可行性筛选，剔除不满足环境有效性、剩余航程、任务时限和接力部署条件的候选方案。",
        "[0013] 步骤四：构建统一评价模型，对直接配送候选方案和接力配送候选方案计算综合成本，并输出可行性标志、成本分解、预计等待时间、预计时间裕度和模式风险值。",
        "[0014] 步骤五：基于统一评价模型构建候选池，并采用ALNS对任务分配、设备匹配、执行模式、路径插入位置和中继点选择进行联合优化。",
        "[0015] 步骤六：在执行过程中监测接力方案状态，当出现电量不足、等待超时、中继点失效、部署不可行、同步偏差过大或时间裕度不足时，触发中继点重选或直接配送回退。",
        "[0016] 步骤七：输出无人机无人车协同配送方案，所述协同配送方案包括任务分配结果、执行模式、中继点位置、无人机执行顺序、无人车配合顺序、综合成本、能耗评估和时序评估结果。",
        "[0017] 具体的，所述步骤一包括以下子步骤：",
        "[0018] 步骤11：读取任务起点、任务终点、载荷、优先级、截止时间、无人机当前位置、无人机剩余电量、无人机最大航程、无人机飞行速度、无人车当前位置、无人车行驶状态以及环境边界、障碍区和禁飞区信息。",
        "[0019] 步骤12：将任务、无人机、无人车和环境约束分别组织为任务集合、无人机集合、无人车集合和环境约束集合。",
        "[0020] 步骤13：对无效坐标、不可执行任务、不可用设备和不满足基本约束的数据进行预处理，形成候选生成和统一评价可直接调用的输入。",
        "[0021] 具体的，所述步骤二包括以下子步骤：",
        "[0022] 步骤21：为每个任务与无人机组合生成直接配送候选方案，所述直接配送候选方案以无人机当前位置作为真实起点，经过任务起点和任务终点，并返回当前锚点或后续衔接位置。",
        "[0023] 步骤22：针对每辆空闲无人车生成接力配送候选方案，候选中继点来源包括无人车当前位置、任务起点、任务终点、任务走廊上的多个采样点以及无人车当前位置到任务走廊线段的投影点。",
        "[0024] 步骤23：对候选中继点按照坐标近似值进行去重，并保留与无人车绑定的可行中继点。",
        "[0025] 具体的，所述步骤三包括以下子步骤：",
        "[0026] 步骤31：判断候选中继点是否位于有效配送区域内，若位于障碍区、禁飞区或环境边界外，则剔除该候选方案。",
        "[0027] 步骤32：判断无人机从当前位置飞往候选中继点所需航程是否小于剩余可用航程，若不满足，则剔除该候选方案。",
        "[0028] 步骤33：判断无人机到达候选中继点后是否仍具有完成中继点、任务起点、任务终点和返回中继点这一配送链路的航程余量，若不满足，则剔除该候选方案。",
        "[0029] 步骤34：当任务设置截止时间时，判断候选方案预计完成时间是否满足任务时限要求，若不满足，则剔除该候选方案。",
        "[0030] 具体的，所述步骤四包括以下子步骤：",
        "[0031] 步骤41：对直接配送候选方案计算无人机飞行时间、无人机能耗、碳排放估计和基础回退风险。",
        "[0032] 步骤42：对接力配送候选方案计算无人机部署段和配送段的飞行时间与能耗、无人车行驶能耗、碳排放估计、双方到达中继点的预计等待时间以及回退风险。",
        "[0033] 步骤43：根据剩余航程裕度、任务时间裕度和无人机无人车同步偏差计算模式风险值。",
        "[0034] 步骤44：按照预设权重对时间成本、无人机能耗、无人车能耗、碳排放估计、等待惩罚和回退风险进行加权，得到候选方案综合成本。",
        "[0035] 具体的，所述步骤五包括以下子步骤：",
        "[0036] 步骤51：对每个任务与无人机组合构建候选池，候选池包括直接配送可行标志和按照综合成本、模式风险、预计等待时间排序后的接力候选方案。",
        "[0037] 步骤52：从候选池中选择前若干个接力候选方案，并优先保持不同无人车来源的候选多样性。",
        "[0038] 步骤53：采用遗憾值插入方式生成初始解；在生成过程中，对每个未分配任务收集可行候选方案，比较最优和次优候选方案之间的成本差异，并优先插入遗憾值较大的任务。",
        "[0039] 步骤54：通过随机移除、最差成本移除和高能耗移除执行破坏操作，通过贪婪插入、遗憾值插入和接力感知遗憾值插入执行修复操作。",
        "[0040] 步骤55：按照自适应权重选择破坏算子和修复算子，并根据新解是否改善当前解或历史最优解更新算子权重。",
        "[0041] 步骤56：采用模拟退火接受准则判断是否接受新解，并在达到最大迭代次数后输出历史最优解。",
        "[0042] 具体的，所述步骤六包括以下子步骤：",
        "[0043] 步骤61：对处于等待无人车状态的任务，检测无人机电量、等待时间、候选中继点有效性和任务剩余时间。",
        "[0044] 步骤62：当触发异常条件时，重新遍历当前可用无人车及其候选中继点，并复用统一评价模型计算各接力候选方案的综合成本、模式风险、预计等待时间和预计时间裕度。",
        "[0045] 步骤63：若存在新的可行接力方案，则选择综合评价最优的中继点和无人车执行重选；若不存在可行接力方案，则评价直接配送方案并在可行时切换为直接配送。",
        "[0046] 步骤64：若新的接力方案和直接配送方案均不可行，则将任务保持为待分配状态，等待后续调度周期重新处理。",
        "[0047] 本发明相对于现有技术具有如下有益效果：第一，将直接配送和接力配送纳入同一候选空间和同一评价模型，避免不同执行模式之间评价口径不一致；第二，在接力方案中显式考虑无人机从当前位置到中继点的部署段，使评分结果更接近真实执行过程；第三，通过候选池和多类ALNS算子降低搜索复杂度，并提升任务分配、设备匹配和中继点选择的联合优化效果；第四，通过等待惩罚、时间裕度和回退风险提高异常情况下方案切换的可解释性；第五，综合考虑无人机能耗、无人车能耗和碳排放估计，有利于形成更符合绿色配送目标的协同调度方案。",
    ]

    for line in reversed(content):
        insert_after(anchor, line)

    doc.save(DOCX_PATH)
    after = validate(DOCX_PATH)
    print({"before": before, "after": after})


if __name__ == "__main__":
    main()
